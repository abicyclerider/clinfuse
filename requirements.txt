# SyntheticMass Python Dependencies
# ===================================
# Core dependencies for synthetic medical dataset generation.
#
# NOTE: PostgreSQL dependencies (psycopg2-binary) are intentionally excluded
# per project constraints - database integration is out of scope.

# Data manipulation and analysis
pandas>=2.0.0
numpy>=1.24.0

# Synthetic data generation
faker>=20.0.0

# Configuration management
pyyaml>=6.0

# Testing framework
pytest>=7.4.0
pytest-cov>=4.1.0

# Property-based testing (for algorithmic validation)
hypothesis>=6.82.0
